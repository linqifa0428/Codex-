# Security Notice

Do not commit or share `auth.json` or any Codex/OpenAI login cache files.

`auth.json` may contain sensitive authentication data. It should only be copied to your own trusted remote server when required for login troubleshooting.
